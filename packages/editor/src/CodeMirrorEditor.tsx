import { useEffect, useRef } from "react";
import { markdown } from "@codemirror/lang-markdown";
import { Compartment, EditorState } from "@codemirror/state";
import { oneDark } from "@codemirror/theme-one-dark";
import { EditorView } from "@codemirror/view";
export interface CodeMirrorEditorProps { value:string; onChange:(value:string)=>void; onSave?:()=>void; theme:"light"|"dark"; readOnly?:boolean; ariaLabel?:string; className?:string; }
const lightTheme=EditorView.theme({"&":{height:"100%",fontSize:"14px",backgroundColor:"transparent"},".cm-scroller":{overflow:"auto",fontFamily:"ui-monospace, SFMono-Regular, Menlo, monospace"},".cm-content":{padding:"16px",caretColor:"currentColor"},".cm-focused":{outline:"2px solid #6366f1",outlineOffset:"-2px"}});
export function CodeMirrorEditor({value,onChange,onSave,theme,readOnly=false,ariaLabel="Markdown source",className}:CodeMirrorEditorProps){const host=useRef<HTMLDivElement>(null);const view=useRef<EditorView>();const themeComp=useRef(new Compartment());const readOnlyComp=useRef(new Compartment());const onChangeRef=useRef(onChange);const onSaveRef=useRef(onSave);onChangeRef.current=onChange;onSaveRef.current=onSave;useEffect(()=>{if(!host.current)return;const state=EditorState.create({doc:value,extensions:[markdown(),themeComp.current.of(theme==="dark"?oneDark:lightTheme),readOnlyComp.current.of([EditorState.readOnly.of(readOnly),EditorView.editable.of(!readOnly)]),EditorView.domEventHandlers({keydown:(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==="s"){event.preventDefault();onSaveRef.current?.();return true}return false}}),EditorView.updateListener.of((update)=>{if(update.docChanged)onChangeRef.current(update.state.doc.toString())})]});const created=new EditorView({state,parent:host.current});created.dom.setAttribute("aria-label",ariaLabel);created.dom.setAttribute("role","textbox");view.current=created;return()=>{created.destroy();view.current=undefined};},[]);useEffect(()=>{const current=view.current;if(current&&current.state.doc.toString()!==value)current.dispatch({changes:{from:0,to:current.state.doc.length,insert:value}})},[value]);useEffect(()=>{view.current?.dispatch({effects:themeComp.current.reconfigure(theme==="dark"?oneDark:lightTheme)})},[theme]);useEffect(()=>{view.current?.dispatch({effects:readOnlyComp.current.reconfigure([EditorState.readOnly.of(readOnly),EditorView.editable.of(!readOnly)])})},[readOnly]);return <div ref={host} className={className} style={{height:"100%"}}/>}
