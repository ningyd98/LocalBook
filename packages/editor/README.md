# @localnote/editor

M2 CodeMirror 6 controlled Markdown source editor with light/dark themes and Ctrl-S callback.

The controlled CodeMirror Markdown editor is implemented here.

- This package owns editor lifecycle, themes, read-only mode and Ctrl-S only; it does not own persistence.
